// adds the number of users to the database
( event_id, u_type, dj_pwd, event_pwd ) 
    if(event_id.exists() && (event_id).count() > 1)
        //generate a new event id and call self again
    if(event_id.exists() && (event_id).count() < 1)
        //use te entry
  if( u_type == 'dj')
        //give more permissions
